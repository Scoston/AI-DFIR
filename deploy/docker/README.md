# Docker Reference

This image is intended for local/staging Workbench validation. Mount case
evidence read-only and place an authenticated gateway/reverse proxy in front of
it. The image intentionally does not install the optional AGPL PyMuPDF profile.
